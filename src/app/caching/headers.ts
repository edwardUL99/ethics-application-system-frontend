/**
 * The header key to reset the cache
 */
export const RESET_CACHE: string = "X-HTTP-Cache-Reset";

/**
 * The number of milliseconds the cache should expire in
 */
export const CACHE_EXPIRY: string = "X-HTTP-Cache-Expiry";