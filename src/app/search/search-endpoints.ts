/**
 * An enum of endpoints that currently support searcj
 */
export enum SearchEndpoints {
  /**
   * The applications endpoint that supports search
   */
  APPLICATIONS = '/api/applications/search',
  /**
   * The users endpoint that supports search
   */
  USERS = '/api/users/search'
}