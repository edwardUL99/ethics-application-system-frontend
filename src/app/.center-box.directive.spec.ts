import { CenterBoxDirective } from './center-box.directive';

describe('CenterBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new CenterBoxDirective();
    expect(directive).toBeTruthy();
  });
});
